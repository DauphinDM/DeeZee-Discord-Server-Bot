"""Command cogs, one per category of the command sheet.

Every ``.py`` file here is loaded automatically at startup by
``core.bot.DeezeeBot._load_cogs``. Files beginning with an underscore are
skipped.
"""
