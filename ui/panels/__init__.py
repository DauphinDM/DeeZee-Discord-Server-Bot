"""Configuration panels.

Each module here owns the presentation of one settings category: the embed that
shows current values, and the buttons, selects and modals that change them. The
cogs own the commands; the panels own what you see.

Kept out of the cogs because a cog that also contains its panel is a four
thousand line file.
"""
